<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css2?family=Jost:wght@400;500;700;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href='/main.css' />
    <title>Walk With Me</title>
</head>
<body>
    <header>
    <nav>
        <div class="nav1">
            <% if(!currentUser)  { %>
            <a href="/"><img src="/images/branding/rplogoGREY.svg" class="logo"></a>
            <% } else { %>
            <a href="/dogs"><img src="/images/branding/rplogoGREY.svg" class="logo"></a>
                <% } %>
        </div>
        <div class="nav2">
 
        </div>
        <div class="nav3">
            <a href="/dogs">Dogs</a>
            <p>|</p>
            <a href="/users">All Volunteers</a>   
            <p>|</p> 
            <% if(!currentUser)  { %>
                <a href="/login">Login</a>
                <p>|</p>
                <a href="/signup">Signup</a>
                        <% } else { %>
                <a href="/users/<%= currentUser._id %>"><%= currentUser.fullName %></a>
                <p>|</p>
                <a href="/logout">Logout</a>
                            <% } %>
        </div>

           
        </nav>
    </header>

